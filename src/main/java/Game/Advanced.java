package Game;

import QuarkEngine.Classes.types.Utils.GraphicsUtils;
import QuarkEngine.QuarkEngineLL;

public class Advanced implements QuarkEngineLL {

    // This Method is called in the Drawing handler, and is specifically used to draw GUI. (durr)
    // All nessessary parameters are passed through the "GraphicsUtils" object.

    // really should only be used for fancy effects.
    @Override
    public void OnGUICall(GraphicsUtils utils) {

    }
}
